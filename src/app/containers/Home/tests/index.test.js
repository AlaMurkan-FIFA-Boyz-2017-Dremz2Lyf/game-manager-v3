import { shallow } from 'enzyme';
import React from 'react';

import HomeContainer from '../index';

xdescribe('<HomeContainer />', () => {
  describe('children', () => {
    it('should render its children inside the main tag', () => {
      // console.log(HomeContainer);
    });
  });
});
